spring:
  application:
    name: test-docker
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      retry:
        initialInterval: 3000
        multiplier: 1.3
        maxInterval: 5000
        maxAttempts: 20
management:
  security:
    enabled: false
